/**
 * `base` is the web app's signature `--mt-radius: 6px`; the rest mirror the
 * `$radius-*` scale.
 */
export const radius = {
  sm: 4,
  base: 6,
  md: 8,
  lg: 16,
  xl: 24,
  pill: 999,
} as const;
